<UserControl x:Class="Wideor.App.Features.Timeline.FilmStripView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Wideor.App.Features.Timeline"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:TimeSpanConverter x:Key="TimeSpanConverter" />
    </UserControl.Resources>
    
    <UserControl.DataContext>
        <Binding RelativeSource="{RelativeSource Self}" Path="ViewModel" />
    </UserControl.DataContext>

    <!-- ListBoxを使用して仮想化を有効化（パフォーマンス向上） -->
    <ListBox x:Name="FilmStripListBox"
             ItemsSource="{Binding ThumbnailItems}"
             ScrollViewer.VerticalScrollBarVisibility="Auto"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             Background="#1A1A1A"
             BorderThickness="0"
             Padding="0"
             VirtualizingPanel.IsVirtualizing="True"
             VirtualizingPanel.VirtualizationMode="Recycling"
             VirtualizingPanel.ScrollUnit="Pixel"
             SelectionMode="Extended">
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <!-- 仮想化時のパフォーマンス向上 -->
                <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type local:ThumbnailItem}">
                    <Border Background="#2A2A2A"
                            BorderBrush="#444444"
                            BorderThickness="0,0,0,1"
                            Padding="4"
                            Height="{Binding DataContext.ThumbnailHeight.Value, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Opacity" Value="1.0" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive.Value}" Value="False">
                                        <Setter Property="Opacity" Value="0.3" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- サムネイル画像 -->
                            <Image Grid.Column="0"
                                   Source="{Binding Thumbnail}"
                                   Width="{Binding DataContext.ThumbnailWidth.Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   Height="{Binding DataContext.ThumbnailHeight.Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   Stretch="Uniform"
                                   Margin="0,0,8,0">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Opacity" Value="1.0" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive.Value}" Value="False">
                                                <Setter Property="Opacity" Value="1.0" />
                                                <!-- Grayscaleエフェクトは後で追加可能 -->
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>

                            <!-- 時間情報 -->
                            <StackPanel Grid.Column="1"
                                       VerticalAlignment="Center">
                                <TextBlock Text="{Binding TimePosition, Converter={StaticResource TimeSpanConverter}}"
                                          Foreground="White"
                                          FontSize="12"
                                          FontWeight="Bold" />
                                <TextBlock Text="{Binding YPosition, StringFormat='Y: {0:F1}px'}"
                                          Foreground="#888888"
                                          FontSize="10"
                                          Margin="0,2,0,0" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>

<UserControl x:Class="Wideor.App.Features.Timeline.TimelinePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Wideor.App.Features.Timeline"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="#1A1A1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ツールバー（ズームコントロールなど） -->
        <Border Grid.Row="0"
                Background="#2A2A2A"
                BorderBrush="#444444"
                BorderThickness="0,0,0,1"
                Padding="8">
            <StackPanel Orientation="Horizontal">
                <Button Content="🔍+"
                       Command="{Binding ZoomInCommand}"
                       Width="40"
                       Height="30"
                       Margin="0,0,4,0"
                       ToolTip="ズームイン" />
                <Button Content="🔍-"
                       Command="{Binding ZoomOutCommand}"
                       Width="40"
                       Height="30"
                       Margin="0,0,8,0"
                       ToolTip="ズームアウト" />
                <TextBlock Text="{Binding PixelsPerSecond.Value, StringFormat='ズーム: {0:F1} px/s'}"
                          Foreground="White"
                          VerticalAlignment="Center"
                          Margin="8,0,0,0" />
            </StackPanel>
        </Border>

        <!-- タイムラインコンテンツ（C1: TimeRuler, C2: FilmStrip） -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" MinWidth="60" MaxWidth="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- C1: TimeRulerView（左側） -->
            <local:TimeRulerView x:Name="TimeRulerView"
                                Grid.Column="0"
                                ViewModel="{Binding}"
                                BorderBrush="#444444"
                                BorderThickness="0,0,1,0"
                                Height="30"
                                VerticalAlignment="Top" />

            <!-- C2: FilmStripView（右側） -->
            <local:FilmStripView x:Name="FilmStripView"
                                Grid.Column="1"
                                ViewModel="{Binding}" />

            <!-- Playhead（赤いインジケータ） -->
            <Border Grid.Column="1"
                    Background="Red"
                    Height="2"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Stretch"
                    Panel.ZIndex="10"
                    IsHitTestVisible="False" />
        </Grid>
    </Grid>
</UserControl>
